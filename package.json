{
  "name": "atomize",
  "private": true,
  "packageManager": "bun@1.2.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && bun run dev",
    "dev:server": "cd apps/server && bun run dev",
    "build": "bun run --cwd packages/game-core build && bun run --cwd apps/server build && bun run --cwd apps/web build",
    "check": "bun run --cwd packages/game-core check && bun run --cwd apps/server check && bun run --cwd apps/web check"
  }
}
